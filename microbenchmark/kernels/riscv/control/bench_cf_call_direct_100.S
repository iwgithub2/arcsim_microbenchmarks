/* bench_cf_call_direct_100: 100 control-flow events on RP2350 Hazard3.
 * Timed code executes from non-striped SRAM8/Scratch X.
 * Generated by generate.py; edit that file to change the set.
 */
#include "riscv/harness.h"

    RISCV_BENCH
    addi sp, sp, -16
    sw ra, 12(sp)
    .rept 100
    .option push
    .option norvc
    jal ra, 2f
    .option pop
    .endr
    lw ra, 12(sp)
    addi sp, sp, 16
    ret
2:  c.nop
    ret
    RISCV_END_BENCH
