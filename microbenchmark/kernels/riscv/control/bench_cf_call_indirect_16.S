/* bench_cf_call_indirect_16: 16 control-flow events on RP2350 Hazard3.
 * Timed code executes from non-striped SRAM8/Scratch X.
 * Generated by generate.py; edit that file to change the set.
 */
#include "riscv/harness.h"

    RISCV_BENCH
    addi sp, sp, -16
    sw ra, 12(sp)
    lla t0, 2f
    .rept 16
    .option push
    .option norvc
    jalr ra, 0(t0)
    .option pop
    .endr
    lw ra, 12(sp)
    addi sp, sp, 16
    ret
2:  c.nop
    ret
    RISCV_END_BENCH
